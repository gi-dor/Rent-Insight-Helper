spring:
  application:
    name: rent-insight

  profiles:
    active: local

  datasource:
    # MySQL ?? ??
    url: jdbc:mysql://localhost:3306/rentinsight?serverTimezone=Asia/Seoul&useUnicode=true&characterEncoding=UTF-8
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    # ?? ??(Entity)? ???? DB ??? ?? ??/??
    hibernate:
      ddl-auto: update
    # ???? ???? SQL? ??? ??
    show-sql: true
    properties:
      hibernate:
        format_sql: true